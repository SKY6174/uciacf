import { CommitteeWorkspace } from "@/components/committee/committee-workspace";

export const metadata = { title: "위원 심의 | UC 산학협력단" };

export default function CommitteePage() {
  return <CommitteeWorkspace />;
}
